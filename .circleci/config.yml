version: 2
jobs:
  build:         
    filters:
      branches:
        ignore:
           - master
    docker:
      - image: hashicorp/terraform
    steps:      
      - checkout
      - run: terraform init
      - run: terraform build
  apply: 
    filters:
      branches:
        only:
           - master
    docker:
      - image: hashicorp/terraform
    steps:
      - checkout
      - run: terraform init
      - run: terraform apply -auto-approve -input=false
<<<<<<< HEAD
workflows:
  version: 2
  plan-and-apply:
    jobs:
      - plan:
          filters:
            branches:
              ignore: master
      - apply:
          filters:
            branches:
              only: master
=======
>>>>>>> clean
